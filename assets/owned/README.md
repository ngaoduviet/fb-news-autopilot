# Owned fallback assets

Place only images owned by Tin Nóng 5s, or images whose licence or permission has
been documented, in this directory. Register each approved file by topic in
`config/image_sources.yaml`. Article `source_image_url` values with `UNKNOWN` rights
are never downloaded or selected automatically.
